import { RangoYearDirective } from './rango-year.directive';

describe('RangoYearDirective', () => {
  it('should create an instance', () => {
    const directive = new RangoYearDirective();
    expect(directive).toBeTruthy();
  });
});
